<h1>Eat Da Burger!</h1>

<h2>The restaurant app that let's you name all of your favorite burgers</h2>

<ul>
    {{#each burgers}}
        <li>
            <p>ID: {{this.id}}</p>
            <p>Burger: {{this.burger_name}}</p>
            <button type="submit">Devour It!</button>
        </li>
    {{/each}}
</ul>

<div>
    <h3>Add a Burger</h3>
    <form class="create-form"></form>
        <div class="form-group">
            <label for="ca">Burger Name:</label>
            <input type="text" id="ca" name="name">
        </div>

        <button type="submit">Add Burger</button>
    </form>
</div>